"""Research engine (sol.md §9–§11).

- parallel_client: thin wrapper over the parallel-web SDK (the load-bearing
  runtime research; stub)
- evidence_normalizer: evidence-integrity invariant + deterministic confidence
  (REAL, tested) and the LLM classification pass (stub)
- evidence_store: reusable evidence persistence (stub; JSON or Firestore)
"""
